<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - OAuth 2.0 Server</title>
    {{> head}}
</head>
<body>
    <div class="login-container">
        <div class="card">
            <h1 style="text-align: center;">🔐 OAuth 2.0 Login</h1>
            
            {{#if error}}
            <div class="alert alert-error">{{error}}</div>
            {{/if}}
            
            <div class="demo-users">
                <h4>Demo Users:</h4>
                <p><code>testuser</code> / <code>password</code></p>
                <p><code>admin</code> / <code>admin123</code></p>
            </div>
            
            <form method="POST" action="/auth/login">
                {{#if returnTo}}
                <input type="hidden" name="returnTo" value="{{returnTo}}">
                {{/if}}
                
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>
                
                <button type="submit" class="btn btn-primary" style="width: 100%; margin: 0;">Sign In</button>
            </form>
        </div>
    </div>
    {{> footer}}
</body>
</html> 